<!-- Conversation Header -->
<div class="d-flex justify-content-between align-items-center mb-3">
  <div>
    <a href="<%= url_for 'sms_index' %>" class="btn btn-outline-secondary btn-sm me-2">
      <%== icon('arrow-left') %> <%== __('Back') %>
    </a>
    <h5 class="d-inline mb-0"><%== __('Conversation with') %> <%= $phone %></h5>
  </div>
  <button class="btn btn-outline-secondary btn-sm" id="syncMessages">
    <%== icon('arrow-repeat') %> <%== __('Sync Messages') %>
  </button>
</div>

<!-- Messages Container -->
<div id="messagesContainer">
  <!-- Messages will be populated by JavaScript -->
</div>