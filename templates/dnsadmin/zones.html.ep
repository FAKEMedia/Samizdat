<a href="<%= url_for('dnsadmin_zone_new') %>" class="btn btn-primary mb-3"><%== __('Create New Zone') %></a>
<table id="zones" class="table table-striped table-sm sortable">
  <thead>
    <tr class="orange">
      <th><%== __('Name') %></th>
      <th><%== __('Actions') %></th>
    </tr>
  </thead>
  <tbody>
    <!-- Zone data will be dynamically populated via AJAX -->
  </tbody>
</table>